# URL of the qBittorrent Web UI to proxy in dev. The Vite dev server rewrites
# Origin/Referer headers to match this target so qBittorrent doesn't reject
# requests. Defaults to http://localhost:8080 if unset.
VITE_QBIT_URL=http://localhost:8080
